apiVersion: v1
kind: Service
metadata:
  name: {{ template "fuse-nifi.fullname" . }}
  labels:
    app: {{ template "fuse-nifi.name" . }}
    chart: {{ template "fuse-nifi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "fuse-nifi.name" . }}
    release: {{ .Release.Name }}
